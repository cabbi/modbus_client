name: modbus_client
description: This package implements a Modbus Client sending requests to a Server (i.e. a remote device) over TCP/IP or Serial port.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^2.17.0

# dependencies:
#   path: ^1.8.0

dev_dependencies:
  lints: ^2.0.0
  test: ^1.21.0
dependencies:
  logging: ^1.2.0
